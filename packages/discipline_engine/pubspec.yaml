name: discipline_engine
description: Pure Dart domain models, state machine, and metrics engine for Habitat.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  meta: ^1.15.0

dev_dependencies:
  test: ^1.25.0
  lints: ^3.0.0
