name: domain
description: Pure Dart domain models, state machines, and business rules for Habitat Discipline Platform.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  meta: ^1.11.0
  uuid: ^4.3.3

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
